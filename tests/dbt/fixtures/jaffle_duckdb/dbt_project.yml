name: jaffle_duckdb
version: "1.0.0"
config-version: 2
profile: jaffle_duckdb

flags:
  send_anonymous_usage_stats: false

model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  jaffle_duckdb:
    staging:
      +materialized: view
    marts:
      +materialized: table
